[project]
name = "hash-checker"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "cloup>=3.0.8",
]

[project.scripts]
checksum = "hash_checker:main"
